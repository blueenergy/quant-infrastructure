apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Standalone worker Deployments (Argo CD / deploy-quant-role.sh). Namespace set by overlay or kubectl -n.
resources:
  - quant-scorer.yaml
  - quant-researcher.yaml
  - quant-portfolio.yaml
  - quant-data-engine.yaml
  - backtest-worker.yaml
  - quant-analyzer.yaml
